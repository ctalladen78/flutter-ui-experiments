import 'package:flutter/material.dart';


class CreateUserFormPageTwo extends StatelessWidget {

  _onNext(){
    // submit form
    // flutter toast display
  }

  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Container(height: 100.0, width: MediaQuery.of(context).size.width,),
    );
  }
}